@import url('https://fonts.googleapis.com/icon?family=Pattaya&display=swap');

.container {
  color: {{ textColor }};

  background-size: cover;
  background-position: center;
  background-image: url({{{ mainImageUrl }}});
}

.overlay {
  width: 100%;
  height: 100%;

  display: flex;
  flex-direction: column;
  align-items: center;

  background-color: {{ backgroundColor }}cc;
}

.title-container {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 80%;
}

#title {
  font-family: Pattaya;
  height: 40%;
  width: 100%;
  text-align: center;
}

.footer {
  display: flex;
  flex-direction: row;
  align-items: center;
  height: 15vh;
  margin-bottom: 3vh;
}

.logo {
  max-width: 5vw;
  height: auto;
}
